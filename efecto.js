// Activa el parpadeo rojo de la tarjeta
function activarAlertaReto() {
    // Solo añade la clase al BODY.
    document.body.classList.add("alerta-reto");
}

// Detiene el parpadeo rojo de la tarjeta
function desactivarAlertaReto() {
    // Solo quita la clase del BODY.
    document.body.classList.remove("alerta-reto");
}